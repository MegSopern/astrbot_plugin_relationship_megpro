{
    "manage_group": {
        "description": "管理群",
        "type": "string",
        "hint": "bot收到的好友申请、群邀请在管理群里审批，不填则将审批信息私发给bot管理员"
    },
    "relationship_admins_id": {
        "description": "管理员",
        "type": "list",
        "hint": "此插件管理员，管理员可审批bot好友申请及群邀请，不填则默认仅继承全局管理员名单",
        "default": []
    },
    "auto_reject_config": {
        "description": "自动审批",
        "type": "object",
        "items": {
            "auto_reject": {
                "description": "拒绝未在依据群聊的用户申请",
                "type": "bool",
                "hint": "开启后，bot收到好友申请和群邀请时，若发起用户未在所设定的依据群聊中，自动拒绝其好友申请和群邀请",
                "default": false
            },
            "auto_reject_group": {
                "description": "自动拒绝功能的依据群聊",
                "type": "string",
                "hint": "设置自动拒绝好友申请和群邀请的依据群聊，若不填则默认使用“管理群”作为依据群聊"
            }
        }
    },
    "temporary_dialogue_control": {
        "description": "临时会话控制",
        "type": "bool",
        "hint": "开启后，用户对bot发起临时会话时，bot不会再进行llm回复",
        "default": false
    },
    "max_ban_duration": {
        "description": "允许的最大禁言时间",
        "type": "int",
        "hint": "单位：秒，bot被禁言超过此时间时将自动退群，推荐值：一天",
        "default": 86400
    },
    "max_group_capacity": {
        "description": "最大群容量",
        "type": "int",
        "hint": "bot加的群若超过此容量将自动退群，可有效防止被乱拉群，推荐值：100",
        "default": 100
    },
    "group_blacklist": {
        "description": "群聊黑名单",
        "type": "list",
        "hint": "群聊黑名单，bot被踢时自动拉黑对应群聊，bot将不会再加入这些群"
    },
    "mutual_blacklist": {
        "description": "互斥成员",
        "type": "list",
        "hint": "互斥成员，bot加入群时，若群内有这些成员，bot将自动退群"
    },
    "auto_check_messages": {
        "description": "是否自动抽查群聊天记录",
        "type": "bool",
        "hint": "开启后，每次触发群管理事件时，bot会自动抽查对应群的聊天记录",
        "default": true
    },
    "new_group_check_delay": {
        "description": "新群延迟抽查时间（秒）",
        "type": "int",
        "hint": "机器人被拉入新群后，等待多少秒再执行自动抽查",
        "default": 30
    }
}